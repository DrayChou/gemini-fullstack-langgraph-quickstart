# API Provider Configuration
# Choose which API provider to use: 'auto', 'openai', or 'gemini'
API_PROVIDER=openai

# OpenAI Configuration (recommended)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://your-proxy-url.com/v1

# Model Override Configuration
# 设置默认使用的模型 - 这样可以轻松切换不同模型
OPENAI_MODEL=deepseek-v3

# Google Gemini Configuration (optional backup)
# GEMINI_API_KEY=your-gemini-key-here
# GEMINI_MODEL=gemini-2.0-flash-exp

# Fine-grained Model Configuration (optional - 可以为不同任务设置不同模型)
# QUERY_GENERATOR_MODEL=deepseek-v3
# REFLECTION_MODEL=gpt-4
# ANSWER_MODEL=deepseek-v3
# REFLECTION_MODEL=gpt-4
# ANSWER_MODEL=gpt-4

# HTTP Proxy Configuration
# Format: protocol://host:port (e.g. http://proxy.example.com:8080)
# HTTP_PROXY=
# HTTPS_PROXY=
